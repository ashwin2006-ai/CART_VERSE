# Local defaults so the app boots without external credentials.
# Real secrets (if any) are delivered via /run/base44/app.env and override these.
DATABASE_URL=mysql://cartverse:cartverse@db:3306/cartverse
JWT_SECRET=aura_luxe_jwt_secret_key_2026_super_secure
NODE_ENV=development
